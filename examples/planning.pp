
function sum(array: *[i32]) -> *[i32] {
    let arr = new Array<>();
}

function main() {
    let array = [10, 20, 4, 2, 1]
}

function linear_search(array: *[i32], size: usize, target: i32) -> i32 {

    for (var i = 0, i < 10, i += 1) {
        if array[i] == target  {
            return i
        }
    }

    return -1
}